# instructions/current — SmartReturn Pro
> 지금 진행 중인 작업 지시문을 둔다. 완료되면 `ai-harness/instructions/completed/`로 옮긴다.

- 파일명: `NNN-<영문슬러그>.md` (작업 단위)
- 신규 화면/기능 지시문은 먼저 docs/specs/ 슬라이스 스펙과 연결한다(게이트 ①~⑤).
- 템플릿은 `ai-harness/instructions/templates/` 참조.
- 모든 경로는 `<PROJECT_ROOT>` 기준 상대경로.
